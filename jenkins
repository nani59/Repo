pipeline {
	agent any

	tools {
	maven"MAVEN"
	}
	stages {
	 stage ('compile')
	 steps {
	  sh "mvn clean compile"
	  }
	  stage ('Test')
	  {
	   sh "mvn clean Test"
	  }
	  stage ('Depoly')
	  { 
	  stage ('Test')
	  {
	   sh "mvn clean Test"
	    }
	   }
	 }
}
